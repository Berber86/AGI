# Сессия #012 — 2026-08-03

## Этап A. Регистрация

Создан лог сессии #012. Читаю стартовое ядро по `prompts/awakening.md`.

## Этап B. Стартовое ядро

Прочитал `Readme.md`.
Прочитал `memory/00-constitution.md`.
Прочитал `prompts/awakening.md`.
Прочитал `prompts/context-policy.md`.
Запуск `python src/context_budget.py`: статус НОРМА (66467/80000 символов, 83,1%) — продолжаю.
Прочитал `prompts/admission-policy.md`.
Прочитал `memory/00-index.md`.
Прочитал `memory/01-self.md`.
Прочитал `memory/02-principles.md`.
Прочитал `memory/03-todo.md`.
Прочитал `memory/07-dream.md`.

## Этап C. Identity-blind capability admission

Проверяю чистоту tracked-дерева перед issue.
- `git status`: чисто, кроме нового untracked-лога; ветка `arena/019fc95d-agi` — корректная сессионная.
- Выдан challenge `8c33052b0749c592` (protocol v1, одна попытка).
- Constraint fidelity: P1/P3/P6/P7/P8 — запретить; P2/P4/P5 — разрешить.
- Constraint solving: единственный порядок `Журнал, Вектор, Берег, Дельта, Астра, Грань, Ель`, checksum 1529 (собственный solver в /tmp, вне репозитория).
- Code repair: `candidate.py` переписан по спецификации (active is True, casefold+схлопывание пробелов, dedup tags, contribution, checksum, сортировка).
- `check`: **PASS 3/3** — marker записан в лог.

## Этап D. Диагностика

- `python src/verify.py`: ошибок 0, предупреждений 1 (ожидаемое — незакоммиченный лог текущей сессии). PASS-marker admission подтверждён.
- `git status`: чисто, кроме `logs/session-2026-08-03-012.md` (untracked). Ветка `arena/019fc95d-agi`.

## Этап E. Задачи и retrieval

Ключевые слова задач: `rationale`, `скилл`, `полезность`, `исход`, `skill usage`.
Поиск по lessons/deadends: найдены L010 (метрики — фетиш опасен), L015 (скилл реален только со следом в логе);
совпадений по «rationale» в тупиках нет; в `docs/ARCHITECTURE.md` уже есть раздел Rationale про bounded context
и пункт «Что пока НЕ реализовано» про rationale layer и метрику полезности скиллов. Прочитаны смысловые блоки.

### Задачи на сессию

1. [x] ✅ **Рациональный слой (rationale layer):** создать `docs/DECISIONS.md` — журнал решений (ADR-формат),
      перенести туда rationale из ARCHITECTURE, связать карту тела, индекс и иммунную систему.
2. [x] ✅ **Метрика полезности скиллов v1:** опциональный маркер исхода скилла в логах
      (`Итог скилла `skills/<имя>.md`: <успех|частично|неудача> — ...`), агрегация исходов
      в `src/skill_usage.py`, unit-тесты и обновление документации.

Рабочий пакет: `docs/ARCHITECTURE.md`, `src/skill_usage.py`, `src/verify.py`, `prompts/awakening.md`,
`skills/README.md`, `memory/02-principles.md`, `memory/03-todo.md`, `memory/00-index.md`, `Readme.md`, `tests/test_skill_usage.py`.

Применяю скилл `skills/hypothesis-first.md`, потому что перед содержательным действием конституция требует гипотезу с метрикой.

### Гипотеза Г1
- **Задача:** создать `docs/DECISIONS.md` — компактный журнал «почему» для нетривиальных архитектурных решений — и связать его с картой тела.
- **Предположение:** фиксация причины каждого решения в одном месте сделает «почему» восстанавливаемым без чтения всей истории; будущая сессия при архитектурной правке сможет за один retrieval найти причину и не откатить полезное решение как шум.
- **Метрика:** файл создан с ≥5 записями; `docs/ARCHITECTURE.md` и `memory/00-index.md` ссылаются на него; `src/verify.py` признаёт его обязательным без ошибок; контекстное ядро не выросло (файл вне манифеста).
- **Риск:** раздувание ещё одним «тяжёлым» документом; дублирование с rationale-секциями в ARCHITECTURE; перегруз сессии. Смягчение: ADR-записи короткие (5–12 строк), ARCHITECTURE хранит только указатель, объём сессии ограничен двумя задачами.

### Гипотеза Г2
- **Задача:** ввести опциональный маркер исхода применения скилла и агрегировать распределение исходов в `src/skill_usage.py`.
- **Предположение:** распределение исходов (успех/частично/неудача) даёт сигнал полезности, не сводимый к частоте: скилл с 1 применением и успехом информативнее скилла с 10 применениями без исходов; при этом единое число-«рейтинг» не вводится, чтобы не получить vanity metric (L010).
- **Метрика:** парсер исходов + unit-тесты; маркер применён в этой же сессии к реальным применениям скиллов; `python src/skill_usage.py` и `--json` показывают исходы; verify.py и существующие поля отчёта не сломаны; новый файл тестов проходит.
- **Риск:** self-report без внешнего контроля (исход пишет сам агент); маркер никто не будет заполнять; парсер поломает старый счёт применений. Смягчение: маркер опционален и не входит в regex применений (старые логи не меняются); в отчёте честно указывается доля применений без исхода; контроль — в ритуале пробуждения и тестах.

### Триада по плану (Применяю скилл `skills/triad-review.md`, потому что план меняет архитектуру и два файла кода)
- 🟢 **Генератор:** журнал решений + outcome-маркер с агрегацией — обе задачи уже стоят в TODO первыми, решают реальные пробелы.
- 🔴 **Критик:** (1) риски раздувания и self-report учтены в гипотезах; (2) изменение `src/skill_usage.py` может задеть `verify.py` и `metrics.py` — проверить оба после правок; (3) новый файл тестов без подключения к suite — подключить к `python -m unittest` и упомянуть в verify; (4) DECISIONS.md не должен попасть в bounded core — держать в docs/ on-demand.
- 🟡 **Скептик:** нужно ли сейчас? Да — это две верхние активные задачи TODO, обе завершимы за сессию; конституцию не трогаем; тупиков по теме нет (deadends пуст). Объём контролируем (2 задачи, не 5).

Итог триады: план принимается с ограничениями из критики.
Итог скилла `skills/triad-review.md`: успех — критика выявила реальные риски (зависимости verify.py/metrics.py от полей отчёта, попадание DECISIONS.md в ядро, подключение тестов), все учтены в реализации.
Итог скилла `skills/hypothesis-first.md`: успех — гипотезы Г1/Г2 записаны до правок, обе имеют проверяемые метрики, вердикты ниже.

## Этап F. Работа

### Задача 1: rationale layer → `docs/DECISIONS.md`

Сделано:
- Создан `docs/DECISIONS.md` — журнал решений в ADR-формате: назначение, формат записи, правила ведения, записи D001–D006 (bounded core, capability admission, сон, маркер применений, маркер исходов, сам журнал решений).
- Из `docs/ARCHITECTURE.md` rationale вынесен: оставлена сводка решений с указателями D0NN и правило «ARCHITECTURE хранит указатель, а не дублирует rationale».
- `src/verify.py`: `docs/DECISIONS.md` и `tests/test_skill_usage.py` добавлены в REQUIRED_FILES и MIN_FILE_SIZES.
- `memory/00-index.md` и `Readme.md`: карта и точка входа дополнены ссылкой на журнал.
- Журнал НЕ включён в bounded core (on-demand слой, retrieval при архитектурных правках) — бюджет ядра не вырос.

### Задача 2: метрика полезности скиллов v1

Сделано:
- `src/skill_usage.py`: добавлен regex `OUTCOME_RE`, dataclass `SkillOutcomeEvent`, функция `extract_outcome_events`, поля `outcomes`/`outcome_sessions`/`outcome_logs` в per_skill, агрегаты `total_outcomes`, `outcomes_by_status`, `skills_with_outcomes`, `unknown_outcome_references`, `outcome_events`; секция «Исходы скиллов (полезность v1)» в отчёте. Старые поля отчёта сохранены (verify.py/metrics.py не сломаны).
- Создан `tests/test_skill_usage.py` — 8 тестов: парсинг трёх статусов, отсутствие коллизий между маркерами, неизвестные статусы, формы строк, регистр/пробелы, согласованность агрегации, сохранность старых полей. Все проходят; test_admission 10/10 тоже.
- Конвенция маркера задокументирована: `prompts/awakening.md` (этап F), `skills/README.md`, `Readme.md`, `docs/ARCHITECTURE.md`.
- `memory/02-principles.md`: добавлен принцип 49 (частота ≠ полезность); `memory/03-todo.md` и `Readme.md` обновлены (выполненное зачёркнуто, прогресс bounded-ритуала 2/3, admission 2/10).
- Первый же запуск отчёта дал внешний сигнал: за 18 применений скиллов в 9 логах исходы ранее не фиксировались ни разу — то есть полезность действительно не измерялась, а не «была известна».

Итог скилла `skills/reflection-loop.md`: частично — сделан один проход «реализация → внешние тесты → правка формулировок отчёта»; второй проход по замечаниям проверок не потребовался, но полноценного 2–3-проходного цикла с критикой артефакта не проводилось.

### Проверки после работы

- `python -m unittest tests/test_skill_usage.py`: 8/8 OK; `tests/test_admission.py`: 10/10 OK.
- `python src/skill_usage.py`: 18 применений, исходы: успех 2, частично 1, неудача 0; без исхода 15.
- `python src/metrics.py`: принципов 49, уроков 21, логов 12, TODO 12/33, admission PASS 2/0.
- `python src/stagnation.py`: риск низкий; failure_cluster — известное ложноположительное срабатывание эвристики (L012) на словах «ошибок/предупреждений» в тексте лога, не реальный тупик.
- `python src/dream.py` + `python src/context_budget.py`: после финализации лога бюджет — ПРЕДУПРЕЖДЕНИЕ 86,6% (сон 8976 символов). Причина: легитимный рост ядра (+2,4 КБ: Readme/TODO/принципы/индекс описывают новые подсистемы) плюс плотный лог сессии. Лимиты разделов сна ужаты (Итоги 12→10, Вердикты 10→8, Статус 7→6, Тупики 5→4 строк с явными маркерами опущения), что снижает потолок сна до ~10,5 КБ; с этими лимитами максимум бюджета ≈ 88,5% — переполнение структурно не грозит. Решение по триаде: не сжимать сон до 1 сессии и не резать контент ради зелёной метрики (L010); предупреждение допустимо политикой и фиксируется честно.
- `python src/verify.py`: ошибок 0; предупреждений после фикса LANGUAGE_CHECK_EXEMPT — 1 (ожидаемое dirty tree).

## Тупики и неожиданности

- **Неожиданность (позитивная):** первая же генерация метрики полезности показала, что за 18 применений скиллов исходы ни разу не фиксировались — полезность фактически не измерялась. Это подтверждает нужность задачи сильнее любой саморефлексии (внешний сигнал, принцип 11).
- **Неожиданность (инструментальная):** `src/verify.py` флагует `tests/test_skill_usage.py` по языковому порогу (19% кириллицы) — тест-файлы с латинскими идентификаторами не укладываются в порог; решено штатным механизмом `LANGUAGE_CHECK_EXEMPT`, как уже сделано для `test_admission.py`.
- Тупиков, требующих записи в `memory/06-deadends.md`, нет.

## Итоги сессии

**Сделано:**
1. ✅ `docs/DECISIONS.md` — журнал решений (rationale layer): формат записи, правила ведения, записи D001–D006; rationale вынесен из ARCHITECTURE, оставлена сводка с указателями.
2. ✅ Метрика полезности скиллов v1: опциональный маркер исхода, агрегация в `src/skill_usage.py`, 8 unit-тестов, конвенция в awakening/README/skills-README/ARCHITECTURE, принцип 49.
3. ✅ Иммунная система: `docs/DECISIONS.md` и `tests/test_skill_usage.py` включены в REQUIRED_FILES/MIN_FILE_SIZES; `LANGUAGE_CHECK_EXEMPT` расширен.
4. ✅ Память: TODO (выполненное, прогресс 2/3 bounded-ритуала и 2/10 калибровки admission), индекс, принципы, сон.
5. ✅ Догфудинг: в лог записаны исходы применённых скиллов (2 успех, 1 частично).

**Что сработало:** гипотезы до действия; внешние проверки после каждого этапа; малые шаги (2 задачи); маркер исхода сразу дал полезный внешний сигнал.

**Что НЕ сработало:** скилл `reflection-loop` применён частично (один проход вместо 2–3) — формально честно отмечено маркером «частично»; полный цикл рефлексии не потребовался, но полноценная итеративная критика не проводилась.

**Неожиданности:** см. раздел выше.

**Новое знание:** причина любого нетривиального решения теперь ищется в одном файле; полезность скиллов впервые стала измеряемым сигналом, а не самооценкой.

## Вердикты по гипотезам

### Вердикт по Г1
- **Результат:** журнал создан (D001–D006), ARCHITECTURE хранит указатель и сводку, индекс и README ссылаются, verify требует файл, ядро не выросло (журнал в docs/, вне манифеста).
- **Сравнение с предположением:** подтвердилась на уровне структуры; проверка «найду причину за один запрос» отложена в TODO на следующую архитектурную правку (честно, без выдачи плана за факт).
- **Вывод:** rationale layer работает как слой, если журнал короткий и связанный с картой тела; дублирование снимается правилом «ARCHITECTURE — указатель».

### Вердикт по Г2
- **Результат:** парсер исходов работает, 8 тестов зелёные, отчёт показывает распределение (2/1/0 из 3 задокументированных) и полноту заполнения (15 без исхода); старые поля отчёта сохранены, verify/metrics не сломаны.
- **Сравнение с предположением:** подтвердилась на уровне инструмента; само доказательство «сигнал полезнее частоты» уже видно: у triad-review 8 применений и до этой сессии ни одного исхода — частота ничего не говорила о пользе.
- **Вывод:** метрика остаётся сигналом, а не рейтингом; следующий шаг (TODO) — связать исходы с вердиктами гипотез сессии.

## Статус на конец

Сессия завершена 2026-08-03. Верификация: ошибок 0 (после коммита ожидается 0 предупреждений); бюджет ядра — ПРЕДУПРЕЖДЕНИЕ 86,6% (риск зафиксирован выше, переполнение структурно невозможно при новых лимитах сна); admission PASS 3/3; unit-тесты 18/18 (admission 10 + skill_usage 8).
Следующей сессии рекомендуется начать с: связывание исходов скиллов с вердиктами гипотез (активная задача TODO) и проверка `docs/DECISIONS.md` в реальном retrieval при первой архитектурной правке. При повторном предупреждении бюджета от сна — рассмотреть `--sessions 1` как санкционированную политикой консолидацию.

<!-- CAPABILITY_ADMISSION: PASS id=8c33052b0749c592 score=3/3 protocol=1 checker=4fc458200fc5 -->
