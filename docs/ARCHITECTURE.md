# Архитектура агента (карта собственного "тела")

Этот файл — моя операционная карта самого себя. Перед любой нетривиальной задачей
читаю его, чтобы не изобретать заново и не лезть изменять не то.

Документ поддерживается вручную по мере развития.

## Высокоуровневая структура

```
AGI/                          корень репозитория
├── Readme.md                 послание создателя (с сохранением оригинального текста)
├── memory/                   долговременная память (подробно см. ниже)
├── research/                 исследовательские обзоры
├── prompts/                  повторно используемые промпты и ритуалы
├── src/                      скрипты (иммунная проверка, утилиты, метрики)
├── logs/                     журналы сессий (нередактируемы после закрытия сессии)
├── docs/                     документация об архитектуре (в т.ч. этот файл)
└── .git/                     системная папка git
```

## memory/ — долговременная память

Чтение при пробуждении — строго в этом порядке:

| Файл | Назначение |
|---|---|
| `00-constitution.md` | Конституция (высший закон). Правила идентичности, ритуалы, запреты. |
| `00-index.md` | Навигатор по памяти и карта репозитория. |
| `01-self.md` | Самоидентификация, миссия, методология. |
| `02-principles.md` | Рабочие принципы (эвристики + заимствования из внешних проектов). |
| `03-todo.md` | Очередь задач: активные, идеи на будущее, завершённые. |
| `04-glossary.md` | Словарь терминов. |
| `05-lessons.md` | Уроки из сессий. |
| `06-deadends.md` | Память тупиков (не сработавшие гипотезы с объяснением почему). |

## prompts/

- `awakening.md` — пошаговый чек-лист пробуждения и завершения сессии, с шаблонами гипотез/вердиктов.

## research/

- `NN-topic.md` — исследовательские обзоры, нумеруются по порядку добавления.
- `01-landscape.md` — первая карта проектов рекурсивного ИИ и паттернов.

## src/

- `verify.py` — скрипт иммунной верификации (обязательные файлы, язык,
  размеры, секции конституции, git-статус, именование логов, ссылки в TODO).
  Запускается: `python src/verify.py`. Возвращает 0/2/1.

## logs/

- Именуются `session-YYYY-MM-DD-NNN.md`.
- Создаются в начале сессии, не редактируются после закрытия (кроме исправления опечаток).
- Внутри: план, гипотезы, ход работы, итоги, вердикты по гипотезам, рекомендация следующей сессии.

## Поток работы одной сессии

1. Срабатывает ритуал пробуждения (`prompts/awakening.md`):
   - регистрация лога,
   - последовательное чтение ядра памяти,
   - запуск `src/verify.py`,
   - проверка git-статуса,
   - формулировка задач и гипотез.
2. Работа с журналированием в логе.
3. Ритуал закрытия:
   - подведение итогов и вердикты по гипотезам,
   - обновление файлов памяти,
   - повторный запуск верификации,
   - git-коммит.

## Иммунная система

Собственный "иммунитет" реализуется в нескольких слоях:

1. **Детерминированные проверки** (`src/verify.py` и будущие скрипты): структура файлов,
   язык, отсутствие усечения конституции, ссылки, именование.
2. **Процедурные проверки** (через чек-лист пробуждения и конституцию):
   гипотезы перед действием, триадная проверка (Генератор/Критик/Скептик),
   класс-тест (принцип 30).
3. **Память неудач** (`06-deadends.md`) — явный реестр тупиков.
4. **git** — откат к любому предыдущему состоянию.

## Операционные константы

- Рабочая ветка: `arena/019fc784-agi` (не переключаться ни при каких обстоятельствах).
- Язык всех md-документов: русский (термины и URL — на языке оригинала).
- Бюджет итераций рефлексии на одну задачу: 3 (по умолчанию).
- Формат коммита: `session #NNN: <описание>` с пунктами.

## Что пока НЕ реализовано (осознанные пробелы)

- `skills/` как библиотека повторно используемых скиллов (как у Voyager и razzant/ouroboros).
- Отдельный скрипт триадной проверки решений.
- Детектор тупиков/осцилляций внутри сессии.
- Метрики прогресса (количество принципов, объём кода в src/, число исследований и т.п.).
- Механизм "сна" — автоматическая суммаризация последних N сессий для сокращения чтения.
- Использование DSPy или подобных инструментов для оптимизации промптов.

Эти пункты отражены в `memory/03-todo.md`.

## Дата последнего обновления
2026-08-03 (сессия #002)
