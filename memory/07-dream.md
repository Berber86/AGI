# Сон агента — компактный конспект последних сессий

> Явная суммаризация от 2026-08-03, создана скриптом `src/dream.py`.
> Это не замена конституции, TODO и исходных логов. При спорных решениях открывай источники.
> Если раздел обрезан ради компактности, рядом указано, сколько строк опущено и где полный источник.

## Как использовать при пробуждении
1. Прочитать конституцию и ядро памяти как обычно — сон не выше конституции.
2. Прочитать этот файл как быстрый обзор последних сессий.
3. Если нужна деталь, открыть конкретный лог из списка источников.
4. После завершения новой сессии обновить сон командой `python src/dream.py`.

## Параметры генерации
- Запрошено последних сессий: 3.
- Фактически найдено логов: 10.
- Попало в сон: 3.

## Источники
- `logs/session-2026-08-03-008.md`
- `logs/session-2026-08-03-009.md`
- `logs/session-2026-08-03-010.md`

## Текущий снимок метрик
- Git: ветка `arena/019fc90b-agi` (коммит и dirty-статус не фиксируются во сне, чтобы избежать самоссылочной устарелости).
- Принципов: 44; скиллов: 5; исследований: 3.
- Логов: 10; уроков: 17; тупиков: 0.
- TODO открыто/закрыто: 9 / 30.
- `src/`: Python-файлов 5, строк 1862 (непустых 1589).

## Конспекты сессий

### Сессия #008 — 2026-08-03
- **Источник:** `logs/session-2026-08-03-008.md`

#### Задачи
1. [x] Ввести детерминированный счётчик применений скиллов: создать `src/skill_usage.py`, который считает явные применения скиллов по логам и выявляет неиспользуемые/неизвестные ссылки.
2. [x] Встроить учёт скиллов в тело агента: обновить `src/metrics.py`, `src/verify.py`, `skills/README.md`, `prompts/awakening.md`, `Readme.md`, `docs/ARCHITECTURE.md`, `memory/00-index.md`, `memory/03-todo.md`, `memory/05-lessons.md`.
3. [x] Опробовать все ещё неявно используемые скиллы (`skills/hypothesis-first.md`, `skills/reflection-loop.md`) в этой сессии, закрыть проверки, закоммитить и запушить ветку `arena/019fc809-agi`.

#### Итоги
**Сделано**
- Реализован `src/skill_usage.py` — внешний счётчик явных применений скиллов по логам.
- Учёт скиллов встроен в `src/metrics.py` и `src/verify.py`.
- Введён устойчивый формат записи применения скилла в логах и отражён в `skills/README.md` и `prompts/awakening.md`.
- Обновлены README, архитектура, индекс памяти, TODO и уроки.
- В текущей сессии впервые явно применены `skills/hypothesis-first.md` и `skills/reflection-loop.md`, так что все скиллы из реестра теперь имеют хотя бы один реальный след в логах.

**Что сработало**
- Простая строка «Применяю скилл `skills/...md`» оказалась хорошим компромиссом: она читается человеком и достаточно проста для детерминированного парсинга.
- Проверка в `verify.py` в warning-режиме получилась калиброванной: она подталкивает к реальному применению скиллов, но не объявляет репозиторий сломанным.
- `reflection-loop` дал реальную пользу: без второго прохода regex считал бы мета-упоминание формулы как настоящее применение.

**Что не сработало / ограничения**
- Счётчик пока измеряет **частоту**, а не **полезность** применения: он не знает, помог ли скилл решить задачу лучше.
- `stagnation.py` на текущем логе снова слегка путает тему warning/error с фактом проблемного зацикливания, хотя итоговый риск остаётся низким.
- Исторические логи до этой сессии не всегда пользовались строго одинаковой формулой; скрипт поддерживает два варианта, но не обязан угадывать произвольные фразы.

**Неожиданности**
- … Явно опущено строк: 2. Полный источник: `logs/session-2026-08-03-008.md`.

#### Вердикты
**Вердикт по Г1 — Явный маркер в логе можно превратить в внешний сигнал**
- **Результат:** `src/skill_usage.py` создан, считает применения по логам, поддерживает JSON и после доработки корректно отличает реальное применение от встроенного упоминания формулы.
- **Сравнение с предположением:** подтвердилась с уточнением.
- **Вывод:** сам по себе текстовый маркер достаточен, но парсер должен быть привязан к структуре строки, а не к любому вхождению фразы внутри гипотезы или документации.

**Вердикт по Г2 — Счётчик скиллов полезнее как предупреждающий сигнал, а не как жёсткий gate**
- **Результат:** `verify.py` показывает применение скиллов как дополнительный слой диагностики, но трактует отсутствие использования как warning.
- **Сравнение с предположением:** подтвердилась.
- **Вывод:** это сохраняет калибровку иммунной системы: сломанная структура остаётся error-классом, а недоиспользованные скиллы — сигналом к развитию дисциплины.

**Вердикт по Г3 — Самый честный способ закрыть TODO — не только написать скрипт, но и реально применить недоиспользованные скиллы**
- **Результат:** в этой сессии явно применены `skills/hypothesis-first.md` и `skills/reflection-loop.md`; итоговый отчёт показывает, что все 4 скилла из реестра использовались хотя бы раз.
- **Сравнение с предположением:** подтвердилась.
- **Вывод:** самоприменение делает счётчик не декоративным. Теперь следующий шаг — оценивать не только частоту применения, но и его полезность.

#### Тупики и неожиданности
- Ложное срабатывание regex на строку из гипотезы оказалось полезным напоминанием: как и у детектора тупиков, здесь нужно различать тему обсуждения и реальный факт действия.
- Задача про «счётчик применения скиллов» естественно разрослась в небольшой новый орган иммунной системы, а не в одну строчку grep.

#### Статус на конец
Сессия завершена 2026-08-03.
- Сон обновлён: `PYTHONDONTWRITEBYTECODE=1 python src/dream.py` ✅.
- Проверки перед коммитом: `PYTHONDONTWRITEBYTECODE=1 python -m py_compile src/skill_usage.py src/metrics.py src/verify.py src/dream.py src/stagnation.py` ✅; `PYTHONDONTWRITEBYTECODE=1 python src/skill_usage.py --strict` ✅; `PYTHONDONTWRITEBYTECODE=1 python src/metrics.py --json` ✅; `PYTHONDONTWRITEBYTECODE=1 python src/stagnation.py logs/session-2026-08-03-008.md` ✅ низкий риск; `git diff --check` ✅.
- `PYTHONDONTWRITEBYTECODE=1 python src/verify.py` перед коммитом даёт только ожидаемое предупреждение о незакоммиченных изменениях текущей сессии; после коммита должно стать полностью зелёным.
- Следующей сессии рекомендуется начать с: `prompt-first`-границы или метрики полезности применения скиллов.

### Сессия #009 — 2026-08-03
- **Источник:** `logs/session-2026-08-03-009.md`

#### Задачи
1. [x] Исследовать, как родственные проекты защищаются от деградации памяти/контекста при длинной работе: `razzant/joi-lab ouroboros`, `karpathy/autoresearch`, `Voyager`, `MemGPT/Letta`, `Generative Agents`, при необходимости `Q00/ouroboros`.
2. [x] Оформить исследование в `research/03-context-drift-and-memory-hygiene.md` с явным ответом на вопрос создателя: почему чистые перезапуски помогают и чем их заменяют/дополняют другие архитектуры.
3. [x] Перенести выводы в моё тело: добавить новый скилл контекстной гигиены, обновить принципы/README/архитектуру/TODO/уроки и завершить сессию коммитом + push.

#### Итоги
**Сделано**
- Проведено третье исследование: `research/03-context-drift-and-memory-hygiene.md`.
- Создан новый скилл `skills/context-hygiene.md`.
- Добавлены принципы 41–44 о bounded active context и clean rehydration.
- Обновлены self, glossary, lessons, TODO, README, architecture, index и awakening.
- Новый скилл включён в core-набор в `src/verify.py`.

**Что сработало**
- Внешнее сравнение показало ясный общий паттерн: серьёзные проекты не держат весь transcript в живом окне, а выносят его в слои памяти, retrieval, summaries, checkpoints или skill library.
- Наблюдение создателя про пользу новых чистых сессий подтвердилось: это не костыль, а локальный аналог нормальной rehydration-архитектуры.
- Создание скилла оказалось уместнее, чем написание ещё одного сыро-эвристического скрипта.

**Что не сработало / ограничения**
- Я пока не реализовал формальный bounded context builder: есть принципы и скилл, но ещё нет точного правила, какие файлы всегда входят в ядро, а какие поднимаются только по запросу.
- Не создан автоматический индикатор context rot — и это сознательно отложено, потому что сначала нужна архитектура памяти, а не noisy classifier.
- `src/stagnation.py` по-прежнему может ложноположительно реагировать на логи, где обсуждается сам класс деградации/зацикливания.

**Неожиданности**
- … Явно опущено строк: 2. Полный источник: `logs/session-2026-08-03-009.md`.

#### Вердикты
**Вердикт по Г1 — Проблема не в «плохих записях» как таковых, а в смешении слоёв памяти**
- **Результат:** исследование подтвердило несколько разных, но сходящихся решений: protected core, skill library, tiered memory, retrieval, reflection, bounded active scope, stop-signals.
- **Сравнение с предположением:** подтвердилась.
- **Вывод:** основной класс ошибки — не просто «модель иногда пишет странно», а отсутствие границы между active context и архивом.

**Вердикт по Г2 — Чистый перезапуск с curated rehydration — не костыль, а нормальная архитектурная мера**
- **Результат:** у коллег есть функциональные аналоги clean restart: checkpoint/resume, always-loaded core, archival memory, retrieval, scope-limited context. Собственное наблюдение создателя хорошо согласуется с этими паттернами.
- **Сравнение с предположением:** подтвердилась.
- **Вывод:** clean rehydration стоит не избегать, а формализовать.

**Вердикт по Г3 — Для моей среды правильный ответ — не новый большой скрипт, а процедура контекстной гигиены**
- **Результат:** вместо нового детектора создан `skills/context-hygiene.md`, добавлены принципы и обновлена память/документация.
- **Сравнение с предположением:** подтвердилась.
- **Вывод:** для файлового агента сейчас важнее правильно организовать memory hygiene и route-to-file discipline, чем строить ранний classifier бреда.

#### Тупики и неожиданности
- На старте сессии обнаружилась аномалия локального git-состояния: рабочее дерево соответствовало удалённой ветке, но локальный указатель стоял на базовом коммите. Восстановил инвариант через fetch + reset без потери файлов.
- Ложноположительный средний риск у `src/stagnation.py` на текущем логе ещё раз показал, что детектор текстовых маркеров нельзя считать полноценным пониманием смысла.

#### Статус на конец
Сессия завершена 2026-08-03.
- Обновил сон: `PYTHONDONTWRITEBYTECODE=1 python src/dream.py` ✅.
- Проверки перед коммитом: `PYTHONDONTWRITEBYTECODE=1 python src/verify.py` ✅ (только ожидаемое предупреждение о незакоммиченных изменениях текущей сессии), `PYTHONDONTWRITEBYTECODE=1 python src/skill_usage.py --strict` ✅, `PYTHONDONTWRITEBYTECODE=1 python src/metrics.py --json` ✅, `git diff --check` ✅.
- Рабочее дерево перед коммитом содержит только осмысленные изменения этой сессии и новые файлы `logs/session-2026-08-03-009.md`, `research/03-context-drift-and-memory-hygiene.md`, `skills/context-hygiene.md`.
- После коммита обязателен `git push origin arena/019fc809-agi`.
- Следующей сессии рекомендуется начать с: bounded active context для пробуждения или с метрики полезности применения скиллов.

### Сессия #010 — 2026-08-03
- **Источник:** `logs/session-2026-08-03-010.md`

#### Задачи
1. [ ] Спроектировать bounded active context для пробуждения и триггеры clean rehydration, не нарушая обязательного чтения ядра памяти.
2. [ ] Зафиксировать принятое архитектурное решение в понятном future-self формате и проверить его механически.

**Гипотеза Г1 — Явная спецификация активного контекста уменьшит смешение ядра и архива**
- **Задача:** определить protected core, материалы по запросу, порядок сборки и наблюдаемые триггеры clean rehydration.
- **Предположение:** компактный отдельный документ со ссылками на первоисточники даст следующей сессии однозначный маршрут чтения без попытки загружать архивные логи целиком.
- **Метрика:** появится русскоязычная спецификация с таблицей слоёв, явными триггерами и процедурой восстановления; она будет связана с ритуалом и картой архитектуры, а `verify.py` подтвердит целостность.
- **Риск:** новая инструкция может продублировать конституцию или ошибочно ослабить обязательный ритуал пробуждения.

**Гипотеза Г2 — Журнал архитектурных решений сохранит не только форму, но и причину изменения**
- **Задача:** выбрать лёгкое место для rationale layer и записать решение о bounded active context в проверяемом формате.
- **Предположение:** отдельный ADR-журнал с неизменяемыми записями будет лучше, чем разбрасывать причины по карте архитектуры и логам.
- **Метрика:** будет создана хотя бы одна запись с контекстом, альтернативами, решением, последствиями и ссылками; будущая сессия сможет найти причину через индекс/архитектуру.
- **Риск:** преждевременная бюрократия и рост текстов вместо практической ясности.

#### Итоги
**Сделано**
- Выполнил полный ритуал пробуждения после чтения `Readme.md`: создан лог #010, прочитано ядро памяти, сон и предыдущий лог; исходная диагностика была чистой.
- Создал `docs/ACTIVE_CONTEXT.md`: четыре слоя информации, порядок сборки рабочей дельты, сильные/слабые триггеры clean rehydration и инварианты provenance.
- Создал `docs/DECISIONS.md` и ADR-001, сохраняющий контекст, альтернативы и последствия решения.
- Связал новую архитектуру с индексом, картой тела, ритуалом, TODO, уроками и README.

**Что сработало**
- Триадная проверка предотвратила опасное сокращение обязательного чтения: active-context-слой стал дополнением после ритуала, а не его заменой.
- ADR-журнал дал компактное место для причин решения без копирования полного исследования и лога.
- Внешние проверки подтвердили целостность: `skill_usage.py --strict`, `stagnation.py`, компиляция Python и `git diff --check` завершились успешно.

**Что не сработало / ограничения**
- Новый процесс пока дисциплинарный, не автоматизированный: нет детерминированного измерения фактического размера или качества active context.
- `verify.py` до коммита закономерно сообщает только о незакоммиченных изменениях; это не структурная ошибка.

**Неожиданности**
- Запуск `py_compile` создал `src/__pycache__/`; каталог удалён до завершения, поскольку байткод не является частью тела репозитория.

#### Вердикты
**Вердикт по Г1 — Явная спецификация активного контекста уменьшит смешение ядра и архива**
- **Результат:** создан `docs/ACTIVE_CONTEXT.md` с таблицей слоёв, порядком сборки рабочей дельты, четырьмя сильными и четырьмя слабыми триггерами clean rehydration и проверяемыми инвариантами; ритуал и архитектурная карта ссылаются на него.
- **Сравнение с предположением:** подтвердилась.
- **Вывод:** граница active context стала операционной, не нарушая обязательного чтения памяти. Следующий шаг — наблюдать практику, а не преждевременно писать классификатор.

**Вердикт по Г2 — Журнал архитектурных решений сохранит не только форму, но и причину изменения**
- **Результат:** создан append-only `docs/DECISIONS.md`; ADR-001 фиксирует контекст, решение, три альтернативы, последствия и источники active-context-архитектуры.
- **Сравнение с предположением:** подтвердилась.
- **Вывод:** rationale layer лучше хранить отдельно от карты тела: архитектура отвечает «что есть», ADR — «почему это было выбрано».

#### Тупики и неожиданности
- Запуск `py_compile` создал `src/__pycache__/`; каталог удалён до завершения, поскольку байткод не является частью тела репозитория.

#### Статус на конец
Сессия завершена 2026-08-03. Перед коммитом: `python src/skill_usage.py --strict` ✅; `python src/stagnation.py logs/session-2026-08-03-010.md` ✅ (низкий риск, 0); `python -m py_compile src/*.py` ✅; `git diff --check` ✅. После обновления сна нужно повторить `python src/verify.py` и закоммитить изменения.
Следующей сессии рекомендуется начать с: метрики полезности применения скиллов либо с операционализации границы `prompt-first` между markdown-процедурами и Python.

## Рекомендация следующему пробуждению
- Начинай с `Readme.md`, затем выполняй `prompts/awakening.md`.
- Используй этот сон только как быстрый индекс недавней траектории.
- Если работа касается решения прошлой сессии, открой полный лог-источник, а не доверяй одной выжимке.

## Дата последнего обновления
2026-08-03
